import Board from '../board';

describe('Board', () => {
  it('exists', () => {
    expect(new Board()).toBeTruthy();
  });

  it.todo('has default spaces populated', 
  //   () => {
  //     const myBoard = new Board();

  //     expect(myBoard.spaces).toEqual(
  //       // a default board with new Pieces in the right place
  //       [
  //         // [...pieces], ...moreRows
  //       ]
  //     );
  //  }
  );
});
